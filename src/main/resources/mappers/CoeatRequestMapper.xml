<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jslhrd.yorimichi.mapper.CoeatRequestMapper">

    <!-- @formatter:off -->

    <select id="selectCoeatCountsByIds" resultType="CoeatDTO">
        SELECT
            coeat_id AS id,
            SUM(CASE WHEN status='APPROVED' THEN 1 ELSE 0 END) AS approvedCount,
            SUM(CASE WHEN status IN ('PENDING','APPROVED') THEN 1 ELSE 0 END) AS appliedCount

        FROM coeat_request

        WHERE coeat_id IN
            <foreach collection="coeatIds" item="coeatId" open="(" close=")" separator=",">
                #{coeatId}
            </foreach>

        GROUP BY coeat_id
    </select>

    <select id="selectStatus" resultType="com.jslhrd.yorimichi.enums.CoeatRequestStatus">
        SELECT status
        FROM coeat_request
        WHERE coeat_id = #{coeatId}
            AND user_id = #{userId}
        FOR UPDATE
    </select>

    <select id="countApprovedById" parameterType="long" resultType="int">
        SELECT COUNT(*)
        FROM coeat_request
        WHERE coeat_id = #{coeatId}
            AND status = 'APPROVED'
        FOR UPDATE
    </select>

    <select id="isApproved" resultType="boolean">
        SELECT EXISTS (
            SELECT 1
            FROM coeat_request c
            WHERE c.user_id = #{userId}
              AND c.coeat_id = #{coeatId}
              AND c.status = 'APPROVED'
        )
    </select>

    <select id="isRequester" resultType="boolean">
        SELECT EXISTS (
            SELECT 1
            FROM coeat_request
            WHERE coeat_id = #{coeatId}
                AND user_id = #{userId}
        )
    </select>

    <insert id="insert" parameterType="CoeatRequestDTO">
        INSERT INTO coeat_request (coeat_id, user_id, message)
        VALUES (#{coeatId}, #{userId}, #{message})
    </insert>

    <update id="updateStatus">
        UPDATE coeat_request
        SET status = #{to}
        WHERE coeat_id = #{coeatId}
            AND user_id = #{userId}
            AND status = #{cur}
    </update>

    <!-- @formatter:on -->

</mapper>