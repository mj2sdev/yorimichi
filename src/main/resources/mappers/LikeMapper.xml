<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jslhrd.yorimichi.mapper.LikeMapper">

    <!-- @formatter:off -->

    <select id="selectByUserId" parameterType="long" resultType="StoreDTO">
        SELECT s.*
        FROM store s
        JOIN likes l ON l.root_id = s.id
        WHERE l.user_id = #{userId}
        ORDER BY l.created_at DESC
    </select>

    <insert id="insert" parameterType="LikeDTO">
        INSERT INTO likes (user_id, root_id)
        VALUES (#{userId}, #{rootId})
    </insert>

    <delete id="delete">
        DELETE
        FROM likes
        WHERE user_id = #{userId}
            AND root_id = #{rootId}
    </delete>

    <!-- @formatter:on -->

</mapper>
