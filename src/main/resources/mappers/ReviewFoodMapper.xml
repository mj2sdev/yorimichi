<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jslhrd.yorimichi.mapper.ReviewFoodMapper">

    <!-- @formatter:off -->

    <select id="selectFoodNameByReviewIds" resultType="ReviewFoodDTO">
        SELECT
            rf.review_id,
            f.name AS foodName

        FROM review_food rf

        JOIN food f ON f.id = rf.food_id

        WHERE rf.review_id IN
            <foreach collection="reviewIds" item="reviewId" open="(" close=")" separator=",">
              #{reviewId}
            </foreach>

        ORDER BY f.name
    </select>

    <insert id="insert" parameterType="ReviewFoodDTO">
        INSERT INTO review_food (review_id, food_id)
        VALUES (#{reviewId}, #{foodId})
    </insert>

    <delete id="delete" parameterType="ReviewFoodDTO">
        DELETE
        FROM review_food
        WHERE review_id = #{reviewId}
          AND food_id = #{foodId}
    </delete>

    <!-- @formatter:on -->

</mapper>