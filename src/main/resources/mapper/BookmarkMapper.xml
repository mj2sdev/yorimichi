<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jslhrd.yorimichi.mapper.BookmarkMapper">

    <insert id="insert" parameterType="BookmarkDTO">
        INSERT INTO bookmark (user_id, feed_id)
        VALUES (#{userId}, #{feedId})
    </insert>

    <select id="selectByUserId" parameterType="long" resultType="BookmarkDTO">
        SELECT * FROM bookmark WHERE user_id = #{userId}
    </select>

    <select id="selectByFeedId" parameterType="long" resultType="BookmarkDTO">
        SELECT * FROM bookmark WHERE feed_id = #{feedId}
    </select>

    <select id="existsByUserIdAndFeedId" resultType="int">
        SELECT COUNT(*) FROM bookmark
        WHERE user_id = #{userId} AND feed_id = #{feedId}
    </select>

    <delete id="deleteById" parameterType="long">
        DELETE FROM bookmark WHERE id = #{id}
    </delete>

    <delete id="deleteByUserIdAndFeedId">
        DELETE FROM bookmark WHERE user_id = #{userId} AND feed_id = #{feedId}
    </delete>

    <delete id="deleteByUserId" parameterType="long">
        DELETE FROM bookmark WHERE user_id = #{userId}
    </delete>

    <delete id="deleteByFeedId" parameterType="long">
        DELETE FROM bookmark WHERE feed_id = #{feedId}
    </delete>

</mapper>
