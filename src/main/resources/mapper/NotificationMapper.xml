<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jslhrd.yorimichi.mapper.NotificationMapper">

    <insert id="insert" parameterType="NotificationDTO">
        INSERT INTO notification (user_id, type, message, read_at)
        VALUES (#{userId}, #{type}, #{message}, #{readAt})
    </insert>

    <select id="selectById" parameterType="long" resultType="NotificationDTO">
        SELECT * FROM notification WHERE id = #{id}
    </select>

    <select id="selectByUserId" parameterType="long" resultType="NotificationDTO">
        SELECT * FROM notification WHERE user_id = #{userId} ORDER BY created_at DESC
    </select>

    <update id="updateReadAt" parameterType="long">
        UPDATE notification
        SET read_at = NOW(), updated_at = NOW()
        WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="long">
        DELETE FROM notification WHERE id = #{id}
    </delete>

    <delete id="deleteByUserId" parameterType="long">
        DELETE FROM notification WHERE user_id = #{userId}
    </delete>

</mapper>
