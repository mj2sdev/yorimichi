<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jslhrd.yorimichi.mapper.StoreFacilityCategoryMapper">

  <insert id="insert" parameterType="com.jslhrd.yorimichi.domain.StoreFacilityCategoryDTO">
    INSERT INTO store_facility_category (store_id, facility_category_id, created_at)
    VALUES (#{storeId}, #{facilityCategoryId}, NOW())
  </insert>

  <delete id="delete" parameterType="com.jslhrd.yorimichi.domain.StoreFacilityCategoryDTO">
    DELETE FROM store_facility_category
    WHERE store_id = #{storeId}
      AND facility_category_id = #{facilityCategoryId}
  </delete>

  <select id="count" parameterType="com.jslhrd.yorimichi.domain.StoreFacilityCategoryDTO" resultType="int">
    SELECT COUNT(1)
    FROM store_facility_category
    WHERE store_id = #{storeId}
      AND facility_category_id = #{facilityCategoryId}
  </select>

  <select id="listByStoreId" parameterType="long"
          resultType="com.jslhrd.yorimichi.domain.StoreFacilityCategoryDTO">
    SELECT store_id, facility_category_id, created_at
    FROM store_facility_category
    WHERE store_id = #{storeId}
    ORDER BY created_at DESC
  </select>

  <select id="listByFacilityCategoryId" parameterType="long"
          resultType="com.jslhrd.yorimichi.domain.StoreFacilityCategoryDTO">
    SELECT store_id, facility_category_id, created_at
    FROM store_facility_category
    WHERE facility_category_id = #{facilityCategoryId}
    ORDER BY created_at DESC
  </select>

</mapper>
