<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jslhrd.yorimichi.mapper.ReviewMapper">

  <select id="selectById" parameterType="long" resultType="com.jslhrd.yorimichi.domain.ReviewDTO">
    SELECT id, user_id, food_id, rating, content
    FROM review
    WHERE id = #{reviewId}
  </select>

  <insert id="insert" parameterType="com.jslhrd.yorimichi.domain.ReviewDTO">
    INSERT INTO review (user_id, food_id, rating, content)
    VALUES (#{userId}, #{foodId}, #{rating}, #{content})
  </insert>

  <update id="update" parameterType="com.jslhrd.yorimichi.domain.ReviewDTO">
    UPDATE review
    SET user_id = #{userId},
        food_id = #{foodId},
        rating  = #{rating},
        content = #{content}
    WHERE id = #{reviewId}
  </update>

  <delete id="deleteById" parameterType="long">
    DELETE FROM review WHERE id = #{reviewId}
  </delete>

  <select id="listByFoodId" parameterType="long" resultType="com.jslhrd.yorimichi.domain.ReviewDTO">
    SELECT id, user_id, food_id, rating, content
    FROM review
    WHERE food_id = #{foodId}
    ORDER BY id DESC
  </select>

  <select id="listByUserId" parameterType="long" resultType="com.jslhrd.yorimichi.domain.ReviewDTO">
    SELECT id, user_id, food_id, rating, content
    FROM review
    WHERE user_id = #{userId}
    ORDER BY id DESC
  </select>

</mapper>
