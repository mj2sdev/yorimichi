<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jslhrd.yorimichi.mapper.UserMapper">

  <select id="selectById" parameterType="long" resultType="com.jslhrd.yorimichi.domain.UserDTO">
    SELECT id, role_id, email, password, nickname, description, last_login_at
    FROM user
    WHERE id = #{userId}
  </select>

  <select id="selectByEmail" parameterType="string" resultType="com.jslhrd.yorimichi.domain.UserDTO">
    SELECT id, role_id, email, password, nickname, description, last_login_at
    FROM user
    WHERE email = #{email}
  </select>

  <insert id="insert" parameterType="com.jslhrd.yorimichi.domain.UserDTO">
    INSERT INTO user (role_id, email, password, nickname, description, last_login_at)
    VALUES (#{roleId}, #{email}, #{password}, #{nickname}, #{description}, #{lastLoginAt})
  </insert>

  <update id="update" parameterType="com.jslhrd.yorimichi.domain.UserDTO">
    UPDATE user
    SET role_id = #{roleId},
        email = #{email},
        password = #{password},
        nickname = #{nickname},
        description = #{description},
        last_login_at = #{lastLoginAt}
    WHERE id = #{userId}
  </update>

  <delete id="deleteById" parameterType="long">
    DELETE FROM user WHERE id = #{userId}
  </delete>

</mapper>
