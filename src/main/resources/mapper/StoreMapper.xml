<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jslhrd.yorimichi.mapper.StoreMapper">

    <insert id="insert" parameterType="StoreDTO">
        INSERT INTO store (owner_id, name, address, description)
        VALUES (#{ownerId}, #{name}, #{address}, #{description})
    </insert>

    <select id="selectById" parameterType="long" resultType="StoreDTO">
        SELECT * FROM store WHERE id = #{id}
    </select>

    <select id="selectByOwnerId" parameterType="long" resultType="StoreDTO">
        SELECT * FROM store WHERE owner_id = #{ownerId}
    </select>

    <select id="selectPaged" resultType="StoreDTO">
        SELECT * FROM store ORDER BY created_at DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <update id="update" parameterType="StoreDTO">
        UPDATE store
        SET name = #{name}, address = #{address}, description = #{description}, updated_at = NOW()
        WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="long">
        DELETE FROM store WHERE id = #{id}
    </delete>

    <delete id="deleteByOwnerId" parameterType="long">
        DELETE FROM store WHERE owner_id = #{ownerId}
    </delete>

</mapper>
