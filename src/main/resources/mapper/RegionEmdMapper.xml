<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jslhrd.yorimichi.mapper.RegionEmdMapper">

    <insert id="insert" parameterType="RegionEmdDTO">
        INSERT INTO region_emd (sigungu_id, code, name)
        VALUES (#{sigunguId}, #{code}, #{name})
    </insert>

    <select id="selectById" parameterType="Long" resultType="RegionEmdDTO">
        SELECT id, sigunguId, code, name, created_at, updated_at
        FROM region_emd
        WHERE id = #{id}
    </select>

    <update id="update" parameterType="RegionEmdDTO">
        UPDATE region_emd
        SET sigungu_id = #{sigunguId}, code = #{code}, name = #{name}
        WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="Long">
        DELETE FROM region_emd WHERE id = #{id}
    </delete>

</mapper>