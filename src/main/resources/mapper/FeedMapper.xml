<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jslhrd.yorimichi.mapper.FeedMapper">

    <insert id="insert" parameterType="FeedDTO">
        INSERT INTO feed (user_id, title, content, visibility)
        VALUES (#{userId}, #{title}, #{content}, #{visibility})
    </insert>

    <select id="selectById" parameterType="long" resultType="FeedDTO">
        SELECT * FROM feed WHERE id = #{id}
    </select>

    <select id="selectByUserId" parameterType="long" resultType="FeedDTO">
        SELECT * FROM feed WHERE user_id = #{userId}
    </select>

    <select id="selectPaged" resultType="FeedDTO">
        SELECT * FROM feed ORDER BY created_at DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <update id="update" parameterType="FeedDTO">
        UPDATE feed
        SET title = #{title}, content = #{content}, visibility = #{visibility}, updated_at = NOW()
        WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="long">
        DELETE FROM feed WHERE id = #{id}
    </delete>

</mapper>
