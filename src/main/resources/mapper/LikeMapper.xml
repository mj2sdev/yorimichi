<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jslhrd.yorimichi.mapper.LikeMapper">

  <!-- id는 AUTO_INCREMENT 가정 -->
  <insert id="insert" parameterType="com.jslhrd.yorimichi.domain.LikeDTO">
    INSERT INTO user_like (user_id, root_id, created_at)
    VALUES (#{userId}, #{rootId}, NOW())
  </insert>

  <delete id="delete" parameterType="com.jslhrd.yorimichi.domain.LikeDTO">
    DELETE FROM user_like
    WHERE user_id = #{userId}
      AND root_id = #{rootId}
  </delete>

  <select id="count" parameterType="com.jslhrd.yorimichi.domain.LikeDTO" resultType="int">
    SELECT COUNT(1)
    FROM user_like
    WHERE user_id = #{userId}
      AND root_id = #{rootId}
  </select>

  <select id="listByUserId" parameterType="long"
          resultType="com.jslhrd.yorimichi.domain.LikeDTO">
    SELECT id, user_id, root_id, created_at
    FROM user_like
    WHERE user_id = #{userId}
    ORDER BY created_at DESC
  </select>

  <select id="listByRootId" parameterType="long"
          resultType="com.jslhrd.yorimichi.domain.LikeDTO">
    SELECT id, user_id, root_id, created_at
    FROM user_like
    WHERE root_id = #{rootId}
    ORDER BY created_at DESC
  </select>

</mapper>
