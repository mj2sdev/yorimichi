<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jslhrd.yorimichi.mapper.RootImageMapper">

  <insert id="insert" parameterType="com.jslhrd.yorimichi.domain.RootImageDTO">
    INSERT INTO root_image (root_id, image_id, created_at)
    VALUES (#{rootId}, #{imageId}, #{createdAt})
  </insert>

  <delete id="delete" parameterType="com.jslhrd.yorimichi.domain.RootImageDTO">
    DELETE FROM root_image
    WHERE root_id = #{rootId}
      AND image_id = #{imageId}
  </delete>

  <select id="count" parameterType="com.jslhrd.yorimichi.domain.RootImageDTO" resultType="int">
    SELECT COUNT(1)
    FROM root_image
    WHERE root_id = #{rootId}
      AND image_id = #{imageId}
  </select>

  <select id="listByRootId" parameterType="long" resultType="com.jslhrd.yorimichi.domain.RootImageDTO">
    SELECT root_id, image_id, created_at
    FROM root_image
    WHERE root_id = #{rootId}
    ORDER BY created_at DESC
  </select>

  <select id="listByImageId" parameterType="long" resultType="com.jslhrd.yorimichi.domain.RootImageDTO">
    SELECT root_id, image_id, created_at
    FROM root_image
    WHERE image_id = #{imageId}
    ORDER BY created_at DESC
  </select>

</mapper>
