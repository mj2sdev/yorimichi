<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jslhrd.yorimichi.mapper.RootImageMapper">

    <insert id="insert" parameterType="RootImageDTO">
        INSERT INTO root_image (root_id, image_id)
        VALUES (#{rootId}, #{imageId})
    </insert>

    <delete id="delete" parameterType="RootImageDTO">
        DELETE FROM root_image
        WHERE root_id = #{rootId} AND image_id = #{imageId}
    </delete>

</mapper>