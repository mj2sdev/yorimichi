<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jslhrd.yorimichi.mapper.CommentMapper">

  <select id="selectById" parameterType="long" resultType="com.jslhrd.yorimichi.domain.CommentDTO">
    SELECT id, user_id, feed_id, parent_id, content
    FROM comment
    WHERE id = #{id}
  </select>

  <select id="selectByFeedId" parameterType="long" resultType="com.jslhrd.yorimichi.domain.CommentDTO">
    SELECT id, user_id, feed_id, parent_id, content
    FROM comment
    WHERE feed_id = #{feedId}
    ORDER BY id ASC
  </select>

  <select id="selectByParentId" parameterType="long" resultType="com.jslhrd.yorimichi.domain.CommentDTO">
    SELECT id, user_id, feed_id, parent_id, content
    FROM comment
    WHERE parent_id = #{parentId}
    ORDER BY id ASC
  </select>

  <insert id="insert" parameterType="com.jslhrd.yorimichi.domain.CommentDTO">
    INSERT INTO comment (user_id, feed_id, parent_id, content)
    VALUES (#{userId}, #{feedId}, #{parentId}, #{content})
  </insert>

  <update id="updateContent" parameterType="com.jslhrd.yorimichi.domain.CommentDTO">
    UPDATE comment
    SET content = #{content}
    WHERE id = #{id}
  </update>

  <delete id="deleteById" parameterType="long">
    DELETE FROM comment WHERE id = #{id}
  </delete>

  <delete id="deleteByFeedId" parameterType="long">
    DELETE FROM comment WHERE feed_id = #{feedId}
  </delete>

</mapper>
