<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jslhrd.yorimichi.mapper.FoodCategoryMapper">

  <insert id="insert" parameterType="com.jslhrd.yorimichi.domain.FoodCategoryDTO">
    INSERT INTO food_category (food_id, category_id, created_at)
    VALUES (#{foodId}, #{categoryId}, NOW())
  </insert>

  <delete id="delete" parameterType="com.jslhrd.yorimichi.domain.FoodCategoryDTO">
    DELETE FROM food_category
    WHERE food_id = #{foodId}
      AND category_id = #{categoryId}
  </delete>

  <select id="count" parameterType="com.jslhrd.yorimichi.domain.FoodCategoryDTO" resultType="int">
    SELECT COUNT(1)
    FROM food_category
    WHERE food_id = #{foodId}
      AND category_id = #{categoryId}
  </select>

  <select id="listByFoodId" parameterType="long" resultType="com.jslhrd.yorimichi.domain.FoodCategoryDTO">
    SELECT food_id, category_id, created_at
    FROM food_category
    WHERE food_id = #{foodId}
    ORDER BY created_at DESC
  </select>

  <select id="listByCategoryId" parameterType="long" resultType="com.jslhrd.yorimichi.domain.FoodCategoryDTO">
    SELECT food_id, category_id, created_at
    FROM food_category
    WHERE category_id = #{categoryId}
    ORDER BY created_at DESC
  </select>

</mapper>
