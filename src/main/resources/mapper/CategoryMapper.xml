<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jslhrd.yorimichi.mapper.CategoryMapper">

    <insert id="insert" parameterType="CategoryDTO">
        INSERT INTO category (parent_id, name, sort_order)
        VALUES (#{parentId}, #{name}, #{sortOrder})
    </insert>

    <select id="selectById" parameterType="long" resultType="CategoryDTO">
        SELECT * FROM category WHERE id = #{id}
    </select>

    <select id="selectByParentId" parameterType="long" resultType="CategoryDTO">
        SELECT * FROM category 
        WHERE (parent_id = #{parentId} OR (#{parentId} IS NULL AND parent_id IS NULL))
        ORDER BY sort_order ASC
    </select>

    <update id="update" parameterType="CategoryDTO">
        UPDATE category
        SET parent_id = #{parentId}, name = #{name}, sort_order = #{sortOrder}, updated_at = NOW()
        WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="long">
        DELETE FROM category WHERE id = #{id}
    </delete>

    <delete id="deleteByParentId" parameterType="long">
        DELETE FROM category WHERE parent_id = #{parentId}
    </delete>

</mapper>
