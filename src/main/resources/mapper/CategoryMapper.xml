<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jslhrd.yorimichi.mapper.CategoryMapper">

  <select id="selectById" parameterType="long" resultType="com.jslhrd.yorimichi.domain.CategoryDTO">
    SELECT id, parent_category_id, name, created_at
    FROM category
    WHERE id = #{categoryId}
  </select>

  <insert id="insert" parameterType="com.jslhrd.yorimichi.domain.CategoryDTO">
    INSERT INTO category (parent_category_id, name, created_at)
    VALUES (#{parentCategoryId}, #{name}, NOW())
  </insert>

  <update id="update" parameterType="com.jslhrd.yorimichi.domain.CategoryDTO">
    UPDATE category
    SET parent_category_id = #{parentCategoryId},
        name = #{name}
    WHERE id = #{categoryId}
  </update>

  <delete id="deleteById" parameterType="long">
    DELETE FROM category WHERE id = #{categoryId}
  </delete>

  <select id="listChildrenOf" parameterType="long" resultType="com.jslhrd.yorimichi.domain.CategoryDTO">
    SELECT id, parent_category_id, name, created_at
    FROM category
    WHERE parent_category_id = #{parentCategoryId}
    ORDER BY name ASC
  </select>

</mapper>
