<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jslhrd.yorimichi.mapper.FacilityCategoryMapper">

  <select id="selectById" parameterType="long" resultType="com.jslhrd.yorimichi.domain.FacilityCategoryDTO">
    SELECT id, name, created_at
    FROM facility_category
    WHERE id = #{facilityCategoryId}
  </select>

  <select id="listAll" resultType="com.jslhrd.yorimichi.domain.FacilityCategoryDTO">
    SELECT id, name, created_at
    FROM facility_category
    ORDER BY id DESC
  </select>

  <insert id="insert" parameterType="com.jslhrd.yorimichi.domain.FacilityCategoryDTO">
    INSERT INTO facility_category (name, created_at)
    VALUES (#{name}, NOW())
  </insert>

  <update id="update" parameterType="com.jslhrd.yorimichi.domain.FacilityCategoryDTO">
    UPDATE facility_category
    SET name = #{name}
    WHERE id = #{facilityCategoryId}
  </update>

  <delete id="deleteById" parameterType="long">
    DELETE FROM facility_category WHERE id = #{facilityCategoryId}
  </delete>

</mapper>
