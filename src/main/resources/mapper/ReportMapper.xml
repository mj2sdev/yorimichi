<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jslhrd.yorimichi.mapper.ReportMapper">

    <insert id="insert" parameterType="ReportDTO">
        INSERT INTO report (reporter_id, root_id, reason)
        VALUES (#{reporter_id}, #{root_id}, #{reason})
    </insert>

    <delete id="delete" parameterType="ReportDTO">
        DELETE
        FROM report
        WHERE reporter_id = #{reporterId}
          AND root_id = #{rootId}
    </delete>

</mapper>