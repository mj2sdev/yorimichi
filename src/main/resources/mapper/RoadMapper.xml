<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jslhrd.yorimichi.mapper.RoadMapper">

    <insert id="insert" parameterType="RoadDTO">
        INSERT INTO road (emd_id, code, name)
        VALUES (#{emdId}, #{code}, #{name})
    </insert>

    <select id="selectById" parameterType="Long" resultType="RoadDTO">
        SELECT id, emd_id, code, name, created_at, updated_at
        FROM road
        WHERE id = #{id}
    </select>

    <update id="update" parameterType="RoadDTO">
        UPDATE road
        SET emd_id = #{emdId},
            code   = #{code},
            name   = #{name}
        WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="Long">
        DELETE
        FROM road
        WHERE id = #{id}
    </delete>

</mapper>