<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jslhrd.yorimichi.mapper.BlockMapper">

    <insert id="insert" parameterType="BlockDTO">
        INSERT INTO block (blocker_id, blocked_id)
        VALUES (#{blockerId}, #{blockedId})
    </insert>

    <select id="selectBlockedUsersByBlockerId" parameterType="long" resultType="BlockDTO">
        SELECT * FROM block WHERE blocker_id = #{blockerId}
    </select>

    <select id="existsByPair" resultType="int">
        SELECT COUNT(*) FROM block
        WHERE blocker_id = #{blockerId} AND blocked_id = #{blockedId}
    </select>

    <delete id="deleteById" parameterType="long">
        DELETE FROM block WHERE id = #{id}
    </delete>

    <delete id="deleteByPair">
        DELETE FROM block WHERE blocker_id = #{blockerId} AND blocked_id = #{blockedId}
    </delete>

    <delete id="deleteByUserId" parameterType="long">
        DELETE FROM block WHERE blocker_id = #{userId} OR blocked_id = #{userId}
    </delete>

</mapper>
