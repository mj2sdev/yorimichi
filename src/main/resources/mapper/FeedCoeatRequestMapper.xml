<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jslhrd.yorimichi.mapper.FeedCoeatRequestMapper">

    <insert id="insert" parameterType="FeedCoeatRequestDTO">
        INSERT INTO feed_coeat_request (coeat_id, requester_id, status)
        VALUES (#{coeatId}, #{requesterId}, #{status})
    </insert>

    <select id="selectById" parameterType="long" resultType="FeedCoeatRequestDTO">
        SELECT * FROM feed_coeat_request WHERE id = #{id}
    </select>

    <select id="selectByCoeatId" parameterType="long" resultType="FeedCoeatRequestDTO">
        SELECT * FROM feed_coeat_request WHERE coeat_id = #{coeatId}
    </select>

    <select id="selectByRequesterId" parameterType="long" resultType="FeedCoeatRequestDTO">
        SELECT * FROM feed_coeat_request WHERE requester_id = #{requesterId}
    </select>

    <select id="existsByCoeatIdAndRequesterId" resultType="int">
        SELECT COUNT(*) FROM feed_coeat_request
        WHERE coeat_id = #{coeatId} AND requester_id = #{requesterId}
    </select>

    <update id="updateStatus" parameterType="FeedCoeatRequestDTO">
        UPDATE feed_coeat_request
        SET status = #{status}, updated_at = NOW()
        WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="long">
        DELETE FROM feed_coeat_request WHERE id = #{id}
    </delete>

    <delete id="deleteByCoeatId" parameterType="long">
        DELETE FROM feed_coeat_request WHERE coeat_id = #{coeatId}
    </delete>

</mapper>
