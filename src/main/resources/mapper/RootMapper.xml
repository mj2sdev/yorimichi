<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jslhrd.yorimichi.mapper.RootMapper">

  <select id="selectById" parameterType="long" resultType="com.jslhrd.yorimichi.domain.RootDTO">
    SELECT id AS root_id, created_at, updated_at, deleted_at, blinded_at
    FROM root
    WHERE id = #{rootId}
  </select>

  <insert id="insert" parameterType="com.jslhrd.yorimichi.domain.RootDTO">
    INSERT INTO root (created_at, updated_at, deleted_at, blinded_at)
    VALUES (#{createdAt}, #{updatedAt}, #{deletedAt}, #{blindedAt})
  </insert>

  <update id="update" parameterType="com.jslhrd.yorimichi.domain.RootDTO">
    UPDATE root
    SET created_at = #{createdAt},
        updated_at = #{updatedAt},
        deleted_at = #{deletedAt},
        blinded_at = #{blindedAt}
    WHERE id = #{rootId}
  </update>

  <delete id="deleteById" parameterType="long">
    DELETE FROM root WHERE id = #{rootId}
  </delete>

</mapper>
