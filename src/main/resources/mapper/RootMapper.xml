<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jslhrd.yorimichi.mapper.RootMapper">

    <insert id="insert" parameterType="RootDTO">
        INSERT INTO root (title, description, type, user_id)
        VALUES (#{title}, #{description}, #{type}, #{userId})
    </insert>

    <select id="selectById" parameterType="long" resultType="RootDTO">
        SELECT * FROM root WHERE id = #{id}
    </select>

    <select id="selectPaged" resultType="RootDTO">
        SELECT * FROM root ORDER BY created_at DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <update id="update" parameterType="RootDTO">
        UPDATE root
        SET title = #{title}, description = #{description}, type = #{type}, updated_at = NOW()
        WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="long">
        DELETE FROM root WHERE id = #{id}
    </delete>

</mapper>
