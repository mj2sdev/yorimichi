spring:
  config:
    activate:
      on-profile: test     # ✅ test 프로파일에서만 적용


  web:
    resources:
      add-mappings: false

  config:
    activate:
      on-profile: test     # ✅ test 프로파일에서만 적용


  web:
    resources:
      add-mappings: false

  datasource:
    url: jdbc:h2:mem:yorimichi;MODE=MySQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    url: jdbc:h2:mem:yorimichi;MODE=MySQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:


  sql:
    init:
      mode: always         # ✅ schema.sql / data.sql 실행 보장

  thymeleaf:
    cache: false
      mode: always         # ✅ schema.sql / data.sql 실행 보장

  thymeleaf:
    cache: false

mybatis:
  # main/resources/mappers/*.xml (네가 만든 경로) 를 읽게 함
  mapper-locations: classpath*:mapper/*.xml
  type-aliases-package: com.jslhrd.yorimichi.domain

logging:
  level:
    com.jslhrd.yorimichi: DEBUG
  type-aliases-package: com.jslhrd.yorimichi.domain

logging:
  level:
    com.jslhrd.yorimichi: DEBUG
